# Web framework
Flask==3.0.3

# Database ORM + migrations
Flask-SQLAlchemy==3.1.1
Flask-Migrate==4.0.7

# Authentication
Flask-JWT-Extended==4.6.0

# Password hashing (comes with Flask/Werkzeug — listed for clarity)
Werkzeug==3.0.3

# CORS support
Flask-Cors==4.0.1

# MySQL driver (C extension — requires libmysqlclient-dev on Ubuntu)
# If mysqlclient fails to compile, use: PyMySQL==1.1.0 instead and see README
mysqlclient==2.2.4

# Environment variable loading
python-dotenv==1.0.1

# Dev/test utilities
pytest==8.1.1
black==24.4.2
flake8==7.0.0
